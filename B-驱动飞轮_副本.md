# Step B：四个驱动飞轮

> AI 原生企业项目管理系统 — 产品方案 Step B
>
> **版本**：v1.2（2026-05-27）
> **阅读对象**：产品、研发、决策层
> **状态**：5 项设计决策已确认（详见第 11 节）；定位调整为**公司内部自用**
> **前置文档**：[A-角色意图卡片.md](./A-角色意图卡片.md)

---

## 0. 文档定位

本文档为 Step B，回答这套系统最根本的问题：

> **"这套系统凭什么转起来？凭什么越用越好？"**

每个飞轮都有五个要素：
- **入口能量**（启动它的初始投入）
- **闭环步骤**（数据/动作如何循环）
- **乘数效应**（"越用越 X" 的具体含义）
- **反向风险**（飞轮可能倒转的场景）
- **防反向机制**（产品里必须内建的保护）

后续步骤：
- Step C：核心产品模块（按钩子映射）
- Step D：关键端到端剧本（一条需求从业务方到交付）

---

## 1. 飞轮总览

四个飞轮，**前后相互喂能量、互相加速**：

```
   ┌─────────────────────────────────────────┐
   │                                         │
   ▼                                         │
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ ① 事实收集飞轮  │ →  │ ② 决策反馈飞轮  │ →  │ ③ 跨层对齐飞轮  │ →  │ ④ 信任飞轮     │
│ (Foundation)  │    │ (Intelligence) │    │ (Scale)        │    │ (Monetization) │
└──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
       │                   │                    │                    │
   集成深度              反馈数据               组织一致性             授权深度
       │                   │                    │                    │
       └───────────────────┴────────────────────┴────────────────────┘
                                      │
                                  数据沉淀回流
```

| 飞轮 | 解决什么 | 入口能量 | 命门 |
|---|---|---|---|
| ① 事实收集 | events 层怎么源源不断丰富 | 集成深度 | 抽取准确率 |
| ② 决策反馈 | AI 怎么越用越懂本公司判断逻辑 | 极简驳回 UX | 模型迭代速度 |
| ③ 跨层对齐 | 全公司怎么基于同一事实协同 | OKR ↔ 项目目标关联 | 各层数据一致性 |
| ④ 信任 | 用户怎么越来越多地把事交给 AI | 第一批高准确率的小事 | 首次重大错误的代价 |

---

## 2. 飞轮 ① 事实收集飞轮（Foundation）

> **核心问题**：events 层怎么源源不断丰富？没有事实，整个系统就是空中楼阁。

### 2.1 飞轮闭环

```
  ① 一线在外部工具里自然干活
        (写代码 / 提 PR / 发消息 / 写文档)
        ↓
  ② AI 自动抽取信号 → events 层
        (commit → 进度信号；聊天 → 阻塞信号；文档变更 → 范围信号)
        ↓
  ③ entities 自动填充，无需手填
        (任务状态自动改 / 关联自动建立)
        ↓
  ④ 一线发现"不用手动同步了"
        ↓
  ⑤ 更愿意在自然工具里干活，不绕开
        ↓
  ⑥ 外部工具使用更频繁 → events 更丰富
        ↓
        回到 ②，抽取准确率上升
```

### 2.2 入口能量

**集成深度**。一期至少 5 类工具深度集成（代码、沟通、文档、设计、测试），缺一类就会有数据黑洞。

### 2.3 乘数效应

**自然工作 ↑ → 数据 ↑ → AI 减少手填 ↑ → 自然工作意愿 ↑**

每多集成一个工具，就少一类数据黑洞；每少一次手填，开发愿意自然工作的比例就上升。

### 2.4 关键角色

- **开发工程师**（事实主力来源 —— 占 events 的 60%+）
- **PO / 业务方**（需求和反馈类 events）
- **AI Agent**（抽取执行者）

### 2.5 反向风险

| 反向场景 | 后果 |
|---|---|
| 抽取错误率高 | 用户怀疑 → 改回手填 → events 减少 → AI 训练数据少 → 错得更多 |
| 集成不到位（缺 GitLab、缺飞书） | 数据缺失 → 用户被迫手填 → 抵触 |
| 用户对"AI 自动改状态"不信任 | 全部锁定为人工 → 飞轮直接停转 |

### 2.6 防反向机制

- **始终保留"我自己改"的入口** —— 不强制用 AI
- **抽取准确率仪表盘公开透明** —— 让用户知道现在 AI 多准
- **首次抽取必须用户确认** —— 准确了一段时间后才转自动
- **集成断连预警** —— 工具集成断了立刻通知管理员

---

## 3. 飞轮 ② 决策反馈飞轮（Intelligence）

> **核心问题**：AI 怎么越用越懂"这家公司"的判断逻辑？不同公司对"项目健康"的定义不同，AI 必须本地化。

### 3.1 飞轮闭环

```
  ① 基于 events，AI 推导 derived insights
        (风险 / 健康度 / 异常状态 / 预测)
        ↓
  ② 管理者 (PM / 负责人) 看到 insights
        ↓
  ③ 一键采纳 或 一键驳回 (可选填原因)
        ↓
  ④ actions 层记录决策
        (谁，对什么 insight，做了什么判断)
        ↓
  ⑤ AI 学习"这家公司这种信号下怎么判断"
        (个性化模型 / 阈值 / 规则微调)
        ↓
  ⑥ 推断越来越本地化、像本团队的判断
        ↓
  ⑦ 采纳率上升 → 信任加深 → 更愿意反馈
        ↓
        回到 ①
```

### 3.2 入口能量

**极简的驳回 UX**。这是整个飞轮的命门 —— **驳回越顺手，反馈数据越多，模型迭代越快**。

理想的驳回交互：
- 一键 "✗ 这不算风险"
- 可选 "✗ 因为这个团队周末本来就不 commit"
- **绝不让用户写长解释**

### 3.3 乘数效应

**驳回数据 > 采纳数据**（金矿）。驳回告诉你 AI 在哪类场景下被否，这比知道它在哪准更有价值。

### 3.4 关键角色

- **PM**（风险预警驳回主力）
- **PO**（需求拆解建议反馈）
- **团队负责人**（派单建议反馈）
- **测试负责人**（放行决策辅助反馈）

### 3.5 反向风险

| 反向场景 | 后果 |
|---|---|
| 驳回流程繁琐（要求填原因） | 用户直接忽略 → AI 不学习 → 一直犯错 |
| AI 不迭代（驳回不影响下次推断） | 一周后还推一样的错 → 用户失去耐心 |
| 推断频次太高（一天 100 条风险） | 用户麻木 → 无差别忽略 |

### 3.6 防反向机制

- **驳回数据进"个性化模型层"，下次推断必参考**
- **推断频次自适应** —— 用户驳回多了自动降频
- **模型版本可见** —— 用户能看到"AI 第几次迭代之后，对你这种场景准了"

### 3.7 个性化模型层的颗粒度（设计决策）

**决策：公司级颗粒度**（公司内自用，一份模型覆盖全公司）。

| 维度 | 公司级（采用） | 团队级（未采用） |
|---|---|---|
| 数据量 | 充分，模型迭代快 | 稀疏，小团队样本不够 |
| 部署成本 | 一份模型，运维简单 | N 份模型，运维复杂 |
| 团队差异适应 | 通过"团队属性特征"喂入模型来区分 | 天然隔离 |
| 跨团队学习 | ✅ 不同团队的反馈互相借鉴 | ❌ 各自为政 |
| 隐私 | 公司内部共享 | 同左 |

**实现要点**：
- 模型输入特征包含"团队 ID / 团队类型 / 项目规模"等，让模型自己学到团队间差异
- 所有反馈数据进同一个模型迭代池（公司内部共享，不存在跨公司问题）

---

## 4. 飞轮 ③ 跨层对齐飞轮（Scale）

> **核心问题**：怎么让一线 / 中层 / 高层基于"同一份事实的不同视角"协同？这是企业级 PM 工具最大的价值，也是组织上最难的事。

### 4.1 飞轮闭环

```
  ① 一线 events (具体事实)
        ↓ 自动聚合
  ② entities (任务状态 / 进度)
        ↓ 自动推导
  ③ derived (项目健康度 / 风险 / 依赖)
        ↓ 自动汇总
  ④ trends (跨项目 / 跨部门趋势 / 战略对齐度)
        ↓
  ⑤ 高层基于 trends 调整战略 / 优先级
        ↓ 战略下达
  ⑥ 中层调整资源 / 范围
        ↓ 任务调整
  ⑦ 一线干新的活
        ↓
        回到 ①，但已经是"被调整后"的任务
```

### 4.2 入口能量

**OKR ↔ 项目目标 ↔ Story 的清晰关联**。没有这个底层关联，各层看到的就是脱节的数据。

### 4.3 乘数效应

**层级越多越能看出"全公司一致性"**。规模越大，飞轮价值越大 —— 这是企业级产品的天然壁垒。

### 4.4 关键角色

- **高管 / 部门负责人**（看 trends、做调整）
- **PMO**（跨项目对齐审视）
- **团队负责人**（中层执行调整）
- **一线**（受调整影响的人）

### 4.5 反向风险

| 反向场景 | 后果 |
|---|---|
| 各层看到的数据"对不上" | 各层不信任，回去用自己的 Excel |
| 战略调整不传导回一线 | 飞轮断裂，一线照旧干 |
| 一线感受不到"我和战略有关" | 抵触填表、抗拒自然工作 |
| 高层只看不动 | 飞轮失去能量来源 |

### 4.6 防反向机制

- **所有层级看到的数据可向下钻取到原始 events**（同一事实的不同视角，可溯源）
- **战略调整必须显式传导**（不是"通知"，而是"任务优先级自动重排 + 一线收到上下文说明"）
- **高层决策可见性** —— 一线能看到"为什么我的任务优先级被调"
- **一致性自动审计** —— **每日批量执行**，不对齐项进 PMO 收件箱

### 4.7 跨层一致性审计的实现（设计决策）

**决策：每日批量审计**（非实时校验）。

| 维度 | 每日批量（采用） | 实时校验（未采用） |
|---|---|---|
| 成本 | 凌晨低峰跑，资源占用低 | 每次写入都校验，资源消耗大 |
| 噪声 | 一日一报，PMO 易处理 | 实时报警，PMO 易麻木 |
| 时效性 | 24 小时内发现 | 秒级发现 |
| 适用场景 | 跨层数据对齐（容忍 24h 滞后） | 关键事务一致性（不容忍） |

**实现要点**：
- 每日凌晨跑跨层 OKR ↔ 项目 ↔ Story 关联完整性、状态一致性、归属一致性等 N 项校验
- 不一致项形成"一致性异常报告"进 PMO 收件箱
- 严重不一致（如 Story 归属冲突）触发 IM 推送给 PMO
- 报告可下钻到具体原始 events，方便溯源修复

---

## 5. 飞轮 ④ 信任飞轮（Adoption）

> **核心问题**：怎么让用户把越来越多的事交给 AI？这个飞轮决定了内部推广的天花板。

### 5.1 飞轮闭环

```
  ① AI 做"小事"
        (状态自动同步 / 日报草稿)
        ↓
  ② 用户验证：准确率高
        ↓
  ③ 信任建立 → 授权 AI 做"中事"
        (自动改非锁定字段 / 代发周报草稿)
        ↓
  ④ 继续验证，依然准
        ↓
  ⑤ 授权 AI 做"大事"
        (派单建议自动派 / 风险自动升级)
        ↓
  ⑥ AI 替代越多人工 → 效率提升越明显
        ↓
  ⑦ **内部推广扩展**：试点团队 → 部门 → 全公司
        ↓
  ⑧ 更多用户数据 → 模型迭代加速
        ↓
        回到 ①，新一轮的"小事"门槛已经抬高
```

### 5.2 入口能量

**第一批"必须高准确率"的低风险动作**。一开始不要做高风险的事 —— 从"自动同步任务状态"这种小事起，准确率必须 > 95%。

### 5.3 乘数效应

**信任越深，AI 替代越多；越多替代，价值越大。**

授权深度的三档（内部用户自主选择，无套餐概念）：
- **基础**：AI 只读、只提示
- **进阶**：AI 改非锁定字段、起草报告
- **深度**：AI 代理执行管理动作（派单建议、自动周报、自动预警分发）

授权默认从"基础"起步，由用户自己升级（飞轮 ④ 启动机制）。

### 5.4 关键角色

- **所有人**（每人决定自己的授权深度）
- **PM / 团队负责人**（典型的"中事 / 大事"授权者）
- **管理员**（公司级授权策略上限设定）

### 5.5 反向风险

| 反向场景 | 后果 |
|---|---|
| 一次重大错误（AI 错改关键字段） | 信任崩塌 → 撤销授权 → 倒退数月 |
| 错误代价不可逆（错发了对外汇报） | 用户不敢再给 AI 任何权 |
| AI 替代了"该是人做"的事（绩效判断） | 用户感觉被冒犯 / 失控 |

### 5.6 防反向机制

- **所有 AI 动作可一键回滚**（撤销改回）
- **高风险动作必须二次确认**（对外发送、跨团队派单）
- **关键动作有"冷静期"**（如自动周报，发送前 30 分钟有窗口可拦截）
- **AI 错误公示板** —— 所有 AI 错误事件公开可见（详见 5.7）
- **绩效类、人事类动作永远不授权给 AI** —— 这条死红线

### 5.7 AI 错误公示板（设计决策）

**决策：必须有 AI 错误公示板**，作为信任建立的核心产品工具。

**核心理念**：**AI 出错不可避免，但出错后的处理方式决定了用户的信任能否恢复**。把错误透明化，反而比掩盖更能建立信任。

**公示板内容**：

| 字段 | 说明 |
|---|---|
| 错误时间 | 发生时间 |
| 错误描述 | AI 做了什么、错在哪 |
| 影响范围 | 哪些实体、哪些用户受影响 |
| 根因 | 是数据问题、模型问题、规则问题 |
| 处理动作 | 已回滚 / 已修复 / 仍在跟进 |
| 改进措施 | 后续如何避免（模型更新、规则补充等） |
| 关联 evidence | 原始 events / 推断链路 |

**可见性策略**：
- **公司全员可见**：基础统计（错误数、影响人数、准确率趋势）
- **管理员 / PMO 可见**：完整错误明细
- **被影响用户**：自动 IM 推送 + 错误详情可见

**关联指标**：
- 错误事件数（目标 < 1 次/月重大错误）
- 错误修复平均时长
- 错误复发率（同类错误是否被根治）

**关键约束**：
- 公示板**不允许任何人删除历史错误**（除合规清理）
- 错误必须**主动发布**，不允许"内部消化"
- 重大错误必须 24 小时内复盘上板

---

## 6. 飞轮间的耦合关系

四个飞轮不是独立的，是**互相喂能量、互相加速**的：

```
飞轮 ①（事实收集） → 喂数据 → 飞轮 ②（决策反馈）
飞轮 ②（决策反馈） → 模型变准 → 飞轮 ④（信任）
飞轮 ④（信任）   → 内部推广扩展 → 反哺飞轮 ① 集成投入
飞轮 ③（跨层对齐） → 让全公司认识到价值 → 加速所有飞轮
飞轮 ③ ← 依赖飞轮 ① 提供事实 + 飞轮 ② 提供洞察
```

### 关键洞察

- **飞轮 ① 是地基** —— 它不转，其他都转不起来
- **飞轮 ④ 是天花板** —— 它转得多快，商业价值多大
- **飞轮 ② 是引擎** —— 它决定了 AI 智能的本地化程度
- **飞轮 ③ 是放大器** —— 它把价值放大到组织级，决定全公司采纳深度

---

## 7. 启动顺序与命门

### 7.1 启动顺序（不能跳）

```
T+0 月:   飞轮 ① 启动（必须先有事实）
T+2 月:   飞轮 ② 接力（有 derived 之后才有反馈）
T+4 月:   飞轮 ④ 加速（信任在反馈中建立）
T+8 月:   飞轮 ③ 飞起来（需要规模化 + 跨项目数据沉淀）
```

### 7.2 三大命门

| 命门 | 描述 | 应对 |
|---|---|---|
| **集成深度** | 缺一类工具就有数据黑洞 | 一期 All-in 5 类工具，宁愿少做功能也要做透集成 |
| **首次错误代价** | AI 第一次重大失误，信任倒退 6 个月 | 保守起步、可回滚、可解释 |
| **驳回 UX 复杂度** | 驳回繁琐 → 飞轮 ② 转不起来 | 一键驳回 + 可选解释，**绝不强制写理由** |

---

## 8. 飞轮在不在转：可观测指标

每个飞轮都要有**可观测指标**，否则就是空话。

### 8.1 飞轮 ① 事实收集

| 指标 | 健康阈值 |
|---|---|
| events 日均流入量 | 持续增长，月环比 +20% |
| AI 抽取准确率 | > 90%（试用期可放宽到 80%） |
| 手动状态修改占比 | 持续下降，目标 < 20% |
| 集成可用性 | > 99%（断连 < 1 小时/月） |

### 8.2 飞轮 ② 决策反馈

| 指标 | 健康阈值 |
|---|---|
| 推断采纳率 | > 50% |
| 推断驳回率 | 15% ~ 30%（太低 = 用户不认真看，太高 = AI 烂） |
| 驳回数据流入"个性化层"覆盖率 | > 80% |
| 模型迭代周期 | 月度 |

### 8.3 飞轮 ③ 跨层对齐

| 指标 | 健康阈值 |
|---|---|
| OKR ↔ 项目 ↔ Story 关联完整度 | > 70% |
| 高层"主动推送"打开率 | > 60% |
| 跨层数据一致性审计通过率 | > 95% |
| 战略调整传导率（从决策到一线任务变化的时间） | < 24 小时 |

### 8.4 飞轮 ④ 信任

| 指标 | 健康阈值 |
|---|---|
| 用户授权深度分布（基础/中/高） | 高授权用户占比月增长 |
| AI 动作回滚率 | < 5% |
| AI 错误事件数 | < 1 次/月（重大错误） |
| 全公司采纳率 / 月活用户数 | 试点期 > 70%，全推后 > 85% |

---

## 9. 飞轮思维对产品设计的指导意义

| # | 设计原则 | 含义 |
|---|---|---|
| 1 | **每个产品功能都要对应一个飞轮** | 不在飞轮里的功能是浪费 |
| 2 | **优先做飞轮 ① 的功能** | 先把地基打牢 |
| 3 | **驳回 / 回滚 / 解释 永远是头等公民** | 这是反向防御 |
| 4 | **所有 AI 输出都必须可溯源** | 这是飞轮 ② 的弹药 |
| 5 | **不做"和飞轮无关"的炫技功能** | 警惕"AI 自动写代码"这种诱惑 |

---

## 10. 飞轮与角色的关联（与 Step A 的映射）

| 飞轮 | 关键角色 | 在飞轮中的作用 |
|---|---|---|
| ① 事实收集 | 开发工程师、PO、业务方、AI Agent | 事实生产者 + 抽取执行者 |
| ② 决策反馈 | PM、PO、负责人、测试负责人 | 反馈数据生产者 |
| ③ 跨层对齐 | 高管、部门负责人、PMO、团队负责人、一线 | 全层级参与 |
| ④ 信任 | 所有角色 + 管理员（公司级上限） | 每个人决定自己的授权深度 |

> **设计推论**：
> - PM / 团队负责人横跨多个飞轮，是产品价值的**结构性中心**
> - 开发工程师虽是 events 主力来源，但他的体验目标是"少用"——所以飞轮 ① 的设计必须最轻
> - 高管在飞轮 ③ / ④ 中是天花板设定者，他的态度决定了全公司采纳广度

---

## 11. 设计决策记录

本节记录评审过程中对 5 个待确认事项的决策，及其对产品形态的影响。

| # | 决策项 | 决策 | 影响章节 | 影响产品形态 |
|---|---|---|---|---|
| 1 | 个性化模型层的颗粒度 | ✅ **公司级**（一个客户公司一份模型） | 3.7 | 一份模型/客户；通过团队属性特征区分团队差异；跨客户绝不共享 |
| 2 | AI 错误的复盘机制 | ✅ **需要"AI 错误公示板"** | 5.7 | 新增独立产品模块；错误透明化、可见、可追溯 |
| 3 | 跨层数据一致性审计频率 | ✅ **每日批量**（非实时） | 4.7 | 凌晨跑校验，异常进 PMO 收件箱；严重不一致触发 IM |
| 4 | 首次启动期是否设"实习中"模式 | ❌ **不需要** | 见下方注释 | AI 一开始就走正常授权流程，**默认授权深度低 + 准确率仪表盘公开** |
| 5 | 商业化套餐与授权深度关系 | ❌ **作废**（v1.2 已定位为内部自用，不做商业化套餐）| — | 授权深度从基础/进阶/深度三档由用户自选 |

### 11.1 决策 4 的补充说明（不设"实习中"模式）

**为什么不需要训练期**：
- 增加产品复杂度（用户要区分"实习中"和"正式"两种状态）
- "实习中"标签反而降低用户信任，与飞轮 ④ 的"信任建立"逻辑相悖
- 已有更好的替代防护机制（见下）

**替代防护机制**（已分散在各飞轮的防反向中）：
- 默认授权深度**低**（只读 / 提示），需要用户**主动升级**
- **抽取准确率仪表盘公开透明**（飞轮 ① 已包含）
- **首次抽取必须用户确认**（飞轮 ① 已包含）
- **所有 AI 动作可一键回滚**（飞轮 ④ 已包含）
- **AI 错误公示板**作为信任建立工具（飞轮 ④ 5.7 新增）

---

## 12. 变更历史

| 版本 | 日期 | 关键变更 |
|---|---|---|
| v1.0 | 2026-05-27 | 初稿：四飞轮 + 耦合 + 启动顺序 + 指标 + 角色映射 |
| v1.1 | 2026-05-27 | 落地 5 项设计决策：①公司级个性化模型 ②AI 错误公示板 ③每日批量一致性审计 ④不设"实习中"模式 ⑤商业化延至独立文档 |
| v1.2 | 2026-05-27 | 定位调整为**公司内部自用**；飞轮 ④ "Monetization" → "Adoption"；商业化套餐作废，授权深度改为用户自选三档；指标改为内部 KPI（采纳率 / 月活）|
